# Installation

```bash
uv sync
uv run main.py
```

# Compiling

```bash
uv run pyinstaller main.py
```

# Add config
```bash
uv run main.py config --api-key YOUR_OPENROUTER_API_KEY
uv run main.py config --user-name YOUR_NAME
uv run main.py config --character-name CHARACTER_NAME
```